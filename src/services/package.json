{
    "name": "services",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "npm run build --prefix create && npm run build --prefix read && npm run build --prefix update && npm run build --prefix delete"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^24.0.10",
        "esbuild": "^0.14.25"
    },
    "dependencies": {
        "@types/aws-lambda": "^8.10.150",
        "@types/uuid": "^10.0.0",
        "aws-sdk": "^2.1692.0",
        "uuid": "^11.1.0"
    }
}
